# figures

- `requirement_tree.png`: 제출 양식 [그림 1]. ITTAGE 예시
- `agent_flow.png`: 제출 양식 [그림 2]. NLGTM Agent flow
- 16:9, 3840×2160. 원본은 같은 이름의 `.html`. 수정 후 아래로 재생성

```
msedge --headless=new --window-size=1920,1080 --force-device-scale-factor=2 --screenshot=<out>.png file:///<path>/<name>.html
```

- `archive/`: D2로 그린 이전 버전 5장
