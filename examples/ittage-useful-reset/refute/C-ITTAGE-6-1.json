{
  "claim": "C-ITTAGE-6-1",
  "verdict": "NO_COUNTEREXAMPLE",
  "trace": "Active config (TLMinimalConfig, ff68f3c5a:src/main/scala/top/Configs.scala:135-142): 3 tables (256,4) (256,8) (512,16), NumBanks=2 (ittage/Parameters.scala:30), TableSramSize=128 (Parameters.scala:38). Per table: setsPerBank = nRows/2 = 128 or 256, setIdxWidth = 7 or 8, idxFullWidth = setIdxWidth+1 (ff68f3c5a:src/main/scala/xiangshan/frontend/bpu/ittage/IttageTable.scala:76-81); foldedWidth = 1 (128-set) or 2 (256-set), dataSplit = 1 (IttageTable.scala:82-83).\n\n[E1 hash convention, predict vs update] Both sides call the same computeTagAndHash (IttageTable.scala:94-110) on getUnhashedIdx(pc) = pc >> instOffsetBits (IttageTable.scala:119): bankIdx = unhashedIdx(bankIdxWidth-1,0) (line 99), setIdxBase = unhashedIdx(bankIdxWidth+setIdxWidth-1, bankIdxWidth) (line 100), setIdx = (setIdxBase ^ idxFh)(setIdxWidth-1,0) (line 101). setIdx is therefore the WITHIN-BANK index, exactly setIdxWidth bits, bank bits stripped before the XOR. Predict: s0 at lines 123-127, s1 regs at 129-130. Update: lines 170-172 give (updateBankIdx, updateIdx, updateTag); updateBankMask = UIntToOH(updateBankIdx, numBanks) (line 172), 1-bit -> 2-bit one-hot. Same function, same slice, no bank bit leaks into the set index.\n\n[G1 update write bank/set] writePort.valid := (io.update.valid && updateBankMask(bankIdx)) || usefulCanReset (IttageTable.scala:212); setIdx := Mux(usefulCanReset, resetSet, updateIdx) (line 214). usefulCanReset = !(io.req.fire || io.update.valid) && needReset (line 182) is mutually exclusive with io.update.valid, so on an update cycle only the hashed bank's buffer gets valid, with setIdx = updateIdx. The non-selected bank's writePort.valid is 0 (updateBankMask one-hot, usefulCanReset false). No other set/bank is written.\n\n[G2 sweep write bank/set] On a usefulCanReset cycle every bank's writePort.valid is 1 with setIdx = resetSet (lines 212, 214). resetSet comes from Counter(usefulCanReset, setsPerBank) (line 184): width log2Ceil(setsPerBank) = setIdxWidth, counts 0..setsPerBank-1 and wraps at setsPerBank-1 (resetFinish). Each bank's SRAM has setsPerBank logical sets (FoldedSRAMTemplate set = setsPerBank, line 140), so the sweep address range is exactly the per-bank index space; no out-of-range value, no wrap into another bank.\n\n[W1 widths along the write path] IttageWriteReq.setIdx = UInt(log2Ceil(nRows).W) instantiated with nRows = setsPerBank (ff68f3c5a:src/main/scala/xiangshan/frontend/bpu/ittage/Bundles.scala:82-85, IttageTable.scala:202) = setIdxWidth. updateIdx is sliced to setIdxWidth (line 101); resetSet is setIdxWidth wide; the Mux at line 214 has no truncation or zero-extension. WriteBuffer stores/returns bits verbatim (ff68f3c5a:src/main/scala/xiangshan/frontend/bpu/WriteBuffer.scala:164, 176, 182, 226). SRAM write: bank.io.w.apply(writeValid, writeEntry, writeSetIdx, true.B, writeBitMask) (IttageTable.scala:225); SRAMBundleA.setIdx = UInt(log2Up(set).W) with set = setsPerBank (utility@0afa3dc SRAMTemplate.scala:26-27; the checked-out utility 58d7554 differs from the pinned 0afa3dc only by removal of resetDone outputs, no change on this path). Widths match, so no implicit truncation.\n\n[W2 folded SRAM address split, 256-set tables] FoldedSRAMTemplate(set=256, width=2, way=1) -> nRows = 128, inner SplittedSRAMTemplate(set=128, way=2) (SRAMTemplate.scala:645-655). Write: waddr = setIdx >> 1, widthIdx = setIdx(0), wmask = UIntToOH(widthIdx) (lines 682-689); bitmask passed unchanged, flattened per way as waymask(n/dataWidth) && bitmask(n%dataWidth) (lines 59-66). So logical set S maps to physical row S>>1, way S&1, and only that way's bits are enabled: the folded neighbour (S^1) in the same physical row has waymask 0 and is untouched. Read uses the same split (raddr = setIdx >> 1, ridx = setIdx(0), lines 665-676), so the mapping is self-consistent. Splitted with setSplit=1: w_setIdx = setIdx.head(innerSetBits=7) = full 7-bit waddr, w_bankSel = 0 (lines 527-532). For 128-set tables width=1: waddr = setIdx, wmask = 1 (lines 684-687).\n\n[G3 SRAM accepts exactly the popped request] writeValid = readPort.valid && !bank.io.r.req.valid (IttageTable.scala:221); readPort.ready = bank.io.w.req.ready && !bank.io.r.req.valid (line 226). io.w.req.ready = !resetHold && !conflictStallWrite (SRAMTemplate.scala:498); conflictStallWrite is only set for BufferWrite/StallWrite behaviours (lines 431-436, 463-467), the bank uses DefaultBehavior = CorruptReadWay (line 177, required for singlePort at line 234), and conflictValidS0 needs r.valid && w.valid in the same cycle (line 294), which line 221 forbids. Hence after power-on w.req.ready is constantly 1 and writeValid implies readPort.ready, so io.read.bits is the real entries(readIdx) (WriteBuffer.scala:225-227), never the DontCare default (line 223). During the SRAM's own power-on reset (resetState, SRAMTemplate.scala:276-290) wen is forced with waddr = resetSet, wdata = 0, full mask (lines 313-330), so a pending buffered write cannot land anywhere; it stays in the buffer (readPort.ready = 0) until reset ends.\n\n[I1 write buffer set hit] Hit/merge in the buffer keys on setIdx (and tag, which IttageWriteReq does not define: WriteReqBundle.tag = None, ff68f3c5a:src/main/scala/xiangshan/frontend/bpu/Abstracts.scala:54-56; WriteBuffer.scala:131-133). A hit rewrites the entry with the new bits including its setIdx (WriteBuffer.scala:176, 182), so the stored request's address always equals the last writer's address. A miss allocates victim = free slot, else PLRU way (lines 157-169): when the buffer is full (io.write.ready ignored at IttageTable.scala:212), a pending write to another set is overwritten and LOST, but the SRAM entry of that other set is left unchanged, so this is a drop (counted by ittage_table_update_drop, IttageTable.scala:272-276, only for update writes; sweep drops are uncounted), not a wrong-set write. Not a counterexample to this isolation claim.\n\nConclusion: for every input class (update on bank 0/1, sweep on any resetSet, 128- or 256-set table, folded way 0/1, buffer empty/hit/full, SRAM in/after power-on reset) the SRAM write address equals the hashed (bank, within-bank set) for updates and (each bank, resetSet) for the sweep, and the waymask/bitmask restricts the write to that single logical entry. Widths: setIdx 7/8 bits end to end; no slicing, wrap, or bank-bit mixing found.\n\nOut-of-scope note for the C-ITTAGE-6-2 refuter (field isolation, not address): IttageEntry declares paddingBit AFTER usefulCnt (Bundles.scala:47-48). Chisel Bundle.asUInt puts the last-declared field at bit 0, so paddingBit is bit 0 and usefulCnt is bit 1, while updateUsBitmask selects bits < UsefulCntWidth = bit 0 only and updateNoUsBitmask selects bits >= 1 (IttageTable.scala:177-179). If that reading is right, the sweep write only touches the padding bit and every usefulCntValid=0 update write does overwrite the useful bit with io.update.usefulCnt. Not verified by elaboration here."
}
